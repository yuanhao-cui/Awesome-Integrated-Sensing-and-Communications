# Exact direct constraints mirrored by pyproject.toml's ``integrity`` group.
# Gate 2 installs its complete transitive environment from the checked uv.lock.
pytest==9.1.1
PyYAML==6.0.2
yamllint==1.35.1
jsonschema==4.25.1
