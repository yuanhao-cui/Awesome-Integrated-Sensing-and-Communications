# Exact direct constraints for baseline-local installs and Dependabot updates.
# The matching ``ci`` group in pyproject.toml is the authoritative declaration;
# uv.lock fixes every transitive dependency and artifact hash for Python 3.10-3.12.
pytest==8.3.5
pytest-cov==6.0.0
numpy==1.26.4
scipy==1.13.1
matplotlib==3.10.9
PyYAML==6.0.2
cvxpy==1.6.5
scikit-learn==1.5.2
torch==2.5.1
jsonschema==4.25.1
yamllint==1.38.0
ruff==0.9.10
